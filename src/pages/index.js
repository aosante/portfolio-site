import * as React from "react"

// styles
const pageStyles = {
  color: "#232129",
  padding: 96,
  fontFamily: "-apple-system, Roboto, sans-serif, serif",
}

export default () => {
  return (
    <main style={pageStyles}>
     INDEX PAGE
    </main>
  )
}
